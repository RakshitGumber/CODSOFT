---
import { getCollection } from "astro:content";
import BlogCard from "../BlogCard/BlogCard.astro";
const blogs = await getCollection("trending");
---

{
  blogs.splice(0, 4).map((blog) => (
    <div class="card-md">
      <BlogCard data={blog} />
    </div>
  ))
}
